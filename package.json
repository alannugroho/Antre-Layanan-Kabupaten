{
  "name": "web-rico-antrian",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev -- --host\"",
    "dev:frontend": "npm --prefix frontend run dev -- --host",
    "dev:backend": "npm --prefix backend run dev",
    "build": "npm --prefix frontend run build && npm --prefix backend run typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
